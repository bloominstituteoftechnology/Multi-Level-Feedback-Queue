const Scheduler=require("./Scheduler"),Process=require("./Process"),main=()=>{const scheduler=new Scheduler;for(let i=1;i<101;i++){let rollForBlockingProcess=Math.random()<.25;scheduler.addNewProcess(new Process(i+1e3,null,rollForBlockingProcess))}scheduler.run()};main();